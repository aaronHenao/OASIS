import { FiltroEscenariosPipe } from './filtro-escenarios.pipe';

describe('FiltroEscenariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEscenariosPipe();
    expect(pipe).toBeTruthy();
  });
});
